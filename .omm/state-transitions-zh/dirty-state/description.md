脏状态（isDirty + 自动保存定时器）——追踪当前标签页是否有未保存的内容变更。
内容变更时立即置 isDirty = true，同时重置 5s 防抖定时器。
自动保存成功后置 isDirty = false，清除标签标题中的修改标记（*）。
手动保存（Ctrl+S）立即触发 IPC write-file 并清除脏状态，跳过防抖等待。
标签切换时将当前脏状态随内容快照一并保存至 tabs[idx]，切回时恢复。
