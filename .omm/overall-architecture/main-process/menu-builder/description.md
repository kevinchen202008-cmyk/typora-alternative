buildMenu() constructs the full native menu template dynamically on every call. Uses m(key) to look up strings from the current menuLocale (loaded from locales/{lang}/menu.json). The theme submenu is built from builtinThemes registry plus any .css files found in userData/themes/. The language submenu is built from loadLocalesIndex(). Menu is rebuilt whenever language or theme changes.
