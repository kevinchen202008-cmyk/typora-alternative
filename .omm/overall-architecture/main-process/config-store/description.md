JSON file at %APPDATA%/typora-alternative/config.json. Fields: theme, language, sidebarOpen, focusMode, typewriterMode, editorFont, editorFontSize, customCssPath, lastFolder, recentFiles[], windowBounds. readConfig() migrates legacy IDs (light→default, dark→night, en→en-US, zh→zh-Hans) on every read. Written with writeConfig() on every updateConfig IPC call and on window close (bounds save).
